STREAMS ONLINE 3.3.6 Rejoin + Copy Fix

기준:
- 학생 재입장이 가능했던 3.3.1 Join Fix 계열을 기준으로 수정했습니다.

수정:
- 같은 이름 재입장 시 기존 보드/currentPlaced 복구
- 같은 기기(clientId 동일)는 이어서 진행
- 다른 기기에서 같은 이름이 online=true이면 입장 차단
- 입장 차단 문구: 이미 같은 이름으로 다른 기기에서 접속 중입니다. 다른 이름을 입력해주세요.
- 방코드 복사 버튼은 room 변수 대신 화면의 방코드 텍스트를 복사하도록 수정
- 버전명이 복사되던 문제 수정
