STREAMS ONLINE 4.0 RC3

핵심 수정:
- Firebase에서 board가 배열이 아니라 객체로 반환되는 경우를 처리하도록 normalizeBoard 추가
- 같은 기기/같은 이름 재입장 시 기존 보드 이어서 진행 안정화
- 다른 기기에서 같은 이름이 online=true이면 입장 차단
- 방코드 복사 버튼 추가
- 학생 목록/전자칠판 보드 보기에서 객체형 board도 처리 가능

테스트 권장:
- 배포 후 새 방 생성
- 학생이 숫자 2~3개 입력
- 같은 기기에서 학생 화면 새로고침 후 같은 이름으로 입장
